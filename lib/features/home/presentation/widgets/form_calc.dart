import 'package:flutter/material.dart';

class FormCalcWidgets {
  Widget squareForm() {
    return Align(alignment: Alignment.center, child: Text('square'));
  }

  Widget cubicForm() {
    return Align(alignment: Alignment.center, child: Text('cubic'));
  }
}
